//declares a variable school and assigns a string value "babcock university"
let school = "babcock university"

const is_babcock_university = school === "babcock university" ? true : false;
console.log(is_babcock_university);